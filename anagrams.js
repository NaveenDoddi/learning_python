var input = ['eat', 'tea', 'tan', 'ate', 'nat', 'bat'];
for(let i = 0; i < input.length; i++){
      var ele = input[i];

      var res = input.filter(j => console.log(j.includes(ele)));
      console.log(ele)

}
